document.addEventListener('input', e=>{if(!e.target.matches('[data-preview]'))return;const target=document.querySelector(e.target.dataset.preview);if(target)target.textContent=e.target.value||target.dataset.empty||''});
document.addEventListener('click', async e=>{const btn=e.target.closest('[data-ai-action]');if(!btn)return;btn.classList.add('ai-loading');btn.disabled=true;const target=document.querySelector(btn.dataset.target);try{const r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:btn.dataset.aiAction,payload:{text:target?.value||target?.textContent||''}})});const json=await r.json();alert(json.message); }finally{btn.classList.remove('ai-loading');btn.disabled=false;}});
async function analyzeJob(){const jd=document.querySelector('#jobDescription')?.value||'';const out=document.querySelector('#jobOutput');if(!out)return;out.innerHTML='<span class="spinner-border spinner-border-sm"></span> Analyzing...';const r=await fetch('/api/job/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({job_description:jd})});const json=await r.json();out.textContent=JSON.stringify(json.data,null,2)}
